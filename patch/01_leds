. /lib/functions/leds.sh
. /lib/functions/uci-defaults.sh

board=$(board_name)

board_config_update

case $board in
beeconmini,seed-ac3)
	ucidef_set_led_netdev "wan" "WAN" "led_wan" "eth0" "link tx rx"
	;;
esac

board_config_flush

exit 0
